This code only measures the displacement from the orign( where the sensor detect the first motion)
to the final location( where the sensor stops moving ).

In addition, this code changes the default frame rate into manual frame rate. 